@model IEnumerable<GoAber.Models.Job>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tasktype)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.schedtype)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cronexp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tasktype)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.schedtype)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cronexp)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
